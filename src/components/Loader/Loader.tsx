import React from "react";

import "./Loader.scss";

const Loader = () => {
    return (
        <div className="waiting__loader__container">
            <span className="waiting__loader"></span>
        </div>
    );
};

export default Loader;
